spring:
    application:
        name: service-ribbon
    cloud:
        config:
            discovery:
                enabled: true
                serviceId: config-jdbc-server
            label: master
            profile: dev