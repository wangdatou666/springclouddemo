spring:
    application:
        name: service-ribbon
    cloud:
        config:
            discovery:
                enabled: true
                serviceId: config-server
            label: master
            profile: dev